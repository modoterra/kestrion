{
  "name": "kestrion",
  "version": "1.33.4",
  "buildHash": "f694531",
  "module": "src/bin/cli.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/bin/dev.ts",
    "dev:cli": "bun run --watch src/bin/cli.tsx",
    "dev:daemon": "bun run --watch src/bin/daemon.tsx",
    "start": "bun run src/bin/cli.tsx",
    "cli": "bun run src/bin/cli.tsx",
    "daemon": "bun run src/bin/daemon.tsx",
    "worker": "bun run src/bin/worker.tsx",
    "kestrion": "bun run src/bin/cli.tsx",
    "kestriond": "bun run src/bin/daemon.tsx",
    "kestrionw": "bun run src/bin/worker.tsx",
    "install": "bun run src/bin/install.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "harness:lint": "oxlint src --deny-warnings",
    "harness:fmt": "oxfmt src",
    "harness:type": "tsc --noEmit",
    "harness:test": "bun run test",
    "harness": "bun run harness:lint && bun run harness:fmt && bun run harness:type && bun run harness:test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.1.97",
    "@opentui/react": "^0.1.97",
    "drizzle-orm": "^0.45.2",
    "react": "^19.2.4"
  }
}
